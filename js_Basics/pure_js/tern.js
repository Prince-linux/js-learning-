// Using the conditional ternary operator in place of if and else statements..

function checkSign(num) {
    return num > 0 ? "Positive" : num < 0 ? "Negative" : "zero"
}

console.log(checkSign(10));